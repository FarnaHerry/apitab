diff --git a/3dparty/SweetEditor b/3dparty/SweetEditor
--- a/3dparty/SweetEditor
+++ b/3dparty/SweetEditor
@@ -1 +1 @@
-Subproject commit bd4330e70eda5c71b71ecac54cf7312d82d2eae2
+Subproject commit bd4330e70eda5c71b71ecac54cf7312d82d2eae2-dirty
diff --git a/CMakeLists.txt b/CMakeLists.txt
index 2405e79..be360cd 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -54,6 +54,7 @@ endif ()
 
 # SweetEditor — third-party cross-platform editor core (C++17, vendored).
 if (NOT HUXERUI_LIBRARY_GRAPH_ONLY)
+    include(FetchContent)
     set(SWEETEDITOR_BUILD_TESTS OFF CACHE BOOL "" FORCE)
     set(SWEETEDITOR_BUILD_STATIC ON CACHE BOOL "" FORCE)
     set(SWEETEDITOR_BUILD_SHARED OFF CACHE BOOL "" FORCE)
@@ -61,11 +62,28 @@ if (NOT HUXERUI_LIBRARY_GRAPH_ONLY)
     set(SWEETEDITOR_BUILD_APPLE_FRAMEWORK OFF CACHE BOOL "" FORCE)
     set(SWEETEDITOR_BUILD_WASM_C_ABI OFF CACHE BOOL "" FORCE)
     set(SWEETEDITOR_BUILD_WASM_EMBIND OFF CACHE BOOL "" FORCE)
-    add_subdirectory(
-        "${CMAKE_CURRENT_SOURCE_DIR}/3dparty/SweetEditor"
-        "${CMAKE_BINARY_DIR}/sweeteditor"
-        EXCLUDE_FROM_ALL
-    )
+    if (NOT EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/3dparty/SweetEditor/CMakeLists.txt")
+        FetchContent_Declare(
+            sweeteditor_source
+            GIT_REPOSITORY https://github.com/FinalScave/SweetEditor.git
+            GIT_TAG bd4330e70eda5c71b71ecac54cf7312d82d2eae2
+            GIT_SHALLOW FALSE
+            # NOTE(apitab local patch): logging.hpp's os_log path is clang-only
+            # (Apple os/log.h hard-errors under GCC). apitab's macOS channel
+            # compiles C++ with brew GCC (import std), so restrict it to clang.
+            # Local nested checkouts must carry the same patch
+            # (cmake/patches/sweeteditor-oslog-gcc.patch in apitab).
+            PATCH_COMMAND bash -c "git apply -R --check '${CMAKE_CURRENT_LIST_DIR}/../../cmake/patches/sweeteditor-oslog-gcc.patch' 2>/dev/null || git apply '${CMAKE_CURRENT_LIST_DIR}/../../cmake/patches/sweeteditor-oslog-gcc.patch'"
+        )
+        FetchContent_MakeAvailable(sweeteditor_source)
+    endif ()
+    if (EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/3dparty/SweetEditor/CMakeLists.txt")
+        add_subdirectory(
+            "${CMAKE_CURRENT_SOURCE_DIR}/3dparty/SweetEditor"
+            "${CMAKE_BINARY_DIR}/sweeteditor"
+            EXCLUDE_FROM_ALL
+        )
+    endif ()
 endif ()
 
 # SweetLine — third-party native syntax highlighting engine (C++17, vendored).
@@ -76,11 +94,22 @@ if (NOT HUXERUI_LIBRARY_GRAPH_ONLY)
     set(SWEETLINE_BUILD_ANDROID_JNI OFF CACHE BOOL "" FORCE)
     set(SWEETLINE_BUILD_WASM_EMBIND OFF CACHE BOOL "" FORCE)
     set(SWEETLINE_BUILD_APPLE_FRAMEWORK OFF CACHE BOOL "" FORCE)
-    add_subdirectory(
-        "${CMAKE_CURRENT_SOURCE_DIR}/3dparty/SweetLine"
-        "${CMAKE_BINARY_DIR}/sweetline"
-        EXCLUDE_FROM_ALL
-    )
+    if (NOT EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/3dparty/SweetLine/CMakeLists.txt")
+        FetchContent_Declare(
+            sweetline_source
+            GIT_REPOSITORY https://github.com/FinalScave/SweetLine.git
+            GIT_TAG 64665d94c8421372cbdce0f74daeb7b8a414d422
+            GIT_SHALLOW FALSE
+        )
+        FetchContent_MakeAvailable(sweetline_source)
+    endif ()
+    if (EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/3dparty/SweetLine/CMakeLists.txt")
+        add_subdirectory(
+            "${CMAKE_CURRENT_SOURCE_DIR}/3dparty/SweetLine"
+            "${CMAKE_BINARY_DIR}/sweetline"
+            EXCLUDE_FROM_ALL
+        )
+    endif ()
 endif ()
 
 file(GLOB_RECURSE LIBRARY_SOURCE_FILES CONFIGURE_DEPENDS
@@ -114,16 +143,26 @@ endif ()
 # editor drags and command taps from raising the keyboard) that released SDKs
 # may not ship. Detect it so the component overrides it only when available.
 set(HUXERUI_HAS_FOCUS_POINTER_HOOK OFF)
+set(HUXERUI_HAS_SCROLL_INPUT_EVENT OFF)
 if (HUXERUI_HOME AND IS_DIRECTORY "${HUXERUI_HOME}/include/huxerui")
     file(GLOB HUXERUI_PUBLIC_HEADERS "${HUXERUI_HOME}/include/huxerui/*.h")
     foreach (HUXERUI_HEADER ${HUXERUI_PUBLIC_HEADERS})
         file(READ "${HUXERUI_HEADER}" HUXERUI_HEADER_CONTENT)
         if (HUXERUI_HEADER_CONTENT MATCHES "ShouldCommitFocusOnPointerUp")
             set(HUXERUI_HAS_FOCUS_POINTER_HOOK ON)
-            break()
+        endif ()
+        if (HUXERUI_HEADER_CONTENT MATCHES "struct ScrollInputEvent")
+            set(HUXERUI_HAS_SCROLL_INPUT_EVENT ON)
         endif ()
     endforeach ()
 endif ()
+if (HUXERUI_HAS_SCROLL_INPUT_EVENT)
+    target_compile_definitions(
+        sweetedit_core
+        PRIVATE
+            SWEETEDIT_CORE_HAS_SCROLL_INPUT_EVENT
+    )
+endif ()
 if (HUXERUI_HAS_FOCUS_POINTER_HOOK)
     target_compile_definitions(
         sweetedit_core
diff --git a/include/sweetedit_core/sweet_editor.h b/include/sweetedit_core/sweet_editor.h
index 114b680..1d0e1e0 100644
--- a/include/sweetedit_core/sweet_editor.h
+++ b/include/sweetedit_core/sweet_editor.h
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <array>
 #include <cstddef>
 #include <cstdint>
 #include <functional>
@@ -15,17 +16,21 @@
 
 namespace sweetedit_huxer {
 
-struct SweetEditorTextChanged : huxerui::Event<> {};
-struct SweetEditorCursorChanged : huxerui::Event<std::uint32_t, std::uint32_t> {};
-struct SweetEditorSelectionChanged : huxerui::Event<std::uint32_t, std::uint32_t, std::uint32_t, std::uint32_t> {};
-struct SweetEditorScrollChanged : huxerui::Event<float, float> {};
-struct SweetEditorFoldToggled : huxerui::Event<std::size_t> {};
-struct SweetEditorLongPressed : huxerui::Event<std::uint32_t, std::uint32_t> {};
-struct SweetEditorDoubleTapped : huxerui::Event<std::uint32_t, std::uint32_t> {};
-struct SweetEditorLinkClicked : huxerui::Event<const std::string&> {};
-struct SweetEditorCodeLensClicked : huxerui::Event<std::int32_t> {};
-struct SweetEditorGutterIconClicked : huxerui::Event<std::uint32_t, std::int32_t> {};
-struct SweetEditorInlayClicked : huxerui::Event<std::uint32_t, std::uint32_t> {};
+// NOTE(apitab local patch): upstream declares events with the old multi-arg
+// Event<Args...> template (HuxerUI SDK 0.1.0). HuxerUI main only accepts a
+// single function-signature parameter (Event<void(Args...)>), so these are
+// rewritten in that form. The emit sites (Emit<Key>(args...)) are unaffected.
+struct SweetEditorTextChanged : huxerui::Event<void()> {};
+struct SweetEditorCursorChanged : huxerui::Event<void(std::uint32_t, std::uint32_t)> {};
+struct SweetEditorSelectionChanged : huxerui::Event<void(std::uint32_t, std::uint32_t, std::uint32_t, std::uint32_t)> {};
+struct SweetEditorScrollChanged : huxerui::Event<void(float, float)> {};
+struct SweetEditorFoldToggled : huxerui::Event<void(std::size_t)> {};
+struct SweetEditorLongPressed : huxerui::Event<void(std::uint32_t, std::uint32_t)> {};
+struct SweetEditorDoubleTapped : huxerui::Event<void(std::uint32_t, std::uint32_t)> {};
+struct SweetEditorLinkClicked : huxerui::Event<void(const std::string&)> {};
+struct SweetEditorCodeLensClicked : huxerui::Event<void(std::int32_t)> {};
+struct SweetEditorGutterIconClicked : huxerui::Event<void(std::uint32_t, std::int32_t)> {};
+struct SweetEditorInlayClicked : huxerui::Event<void(std::uint32_t, std::uint32_t)> {};
 
 namespace detail {
 class SweetEditorControllerState;
@@ -168,6 +173,103 @@ using HostDecorationProvider = std::function<HostDecorationResult(uint32_t start
 // (reference GutterIconProvider): the host can mark breakpoints/errors etc.
 using GutterIconProvider = std::function<std::vector<std::pair<uint32_t, int32_t>>(uint32_t start_line, uint32_t end_line)>;
 
+// NOTE(apitab local patch): every color the editor paints, injectable by the
+// host for theme adaptation. All values are ARGB (0xAARRGGBB). The defaults
+// reproduce the upstream hardcoded light theme exactly, so a
+// default-constructed palette changes nothing. The static_cast<int32_t> on
+// each default keeps MSVC (/std:c++latest, C++26 narrowing-in-constant-
+// conversion) from rejecting the 0x........ literals as non-value-preserving
+// (error C2397); plain code is unaffected.
+struct SweetEditorPalette {
+  int32_t editor_background = static_cast<int32_t>(0xFFFFFFFF);
+  int32_t gutter_background = static_cast<int32_t>(0xFFF2F3F5);
+  // Hairline between the gutter and the text area.
+  int32_t gutter_split_line = static_cast<int32_t>(0xFFE3E5E8);
+  int32_t line_number = static_cast<int32_t>(0xFF9AA0A6);
+  int32_t cursor = static_cast<int32_t>(0xFF1F1F1F);
+  int32_t selection_background = static_cast<int32_t>(0x554A90E2);
+  // Selection drag handles and the HuxerUI selection-overlay accent.
+  int32_t selection_handle = static_cast<int32_t>(0xFF1F6FEB);
+  int32_t current_line_background = static_cast<int32_t>(0x0F000000);
+  int32_t guide = static_cast<int32_t>(0xFFC8C8C8);
+  int32_t separator = static_cast<int32_t>(0xFFB0B7C3);
+  int32_t inlay_hint_background = static_cast<int32_t>(0x143B82F6);
+  int32_t inlay_hint_text = static_cast<int32_t>(0xB0344A73);
+  int32_t fold_placeholder_background = static_cast<int32_t>(0x2E748DB0);
+  int32_t fold_placeholder_text = static_cast<int32_t>(0xFF284A70);
+  int32_t gutter_icon = static_cast<int32_t>(0xFF267F99);
+  // Whitespace rendering: space dots, and the tab-arrow / line-break hooks.
+  int32_t whitespace_marker = static_cast<int32_t>(0x40000000);
+  int32_t whitespace_symbol = static_cast<int32_t>(0x4D000000);
+  int32_t phantom_text = static_cast<int32_t>(0x99999999);
+  int32_t scrollbar_thumb = static_cast<int32_t>(0x73000000);
+  // Completion panel (reference light theme, EditorTheme.java).
+  int32_t completion_panel_background = static_cast<int32_t>(0xF0FAFBFD);
+  int32_t completion_panel_border = static_cast<int32_t>(0x30A0A8B8);
+  int32_t completion_selected_background = static_cast<int32_t>(0x3D3B82F6);
+  int32_t completion_label = static_cast<int32_t>(0xFF1F2937);
+  int32_t completion_detail = static_cast<int32_t>(0xFF8A94A6);
+  int32_t completion_badge_text = static_cast<int32_t>(0xFFFFFFFF);
+  int32_t completion_kind_keyword = static_cast<int32_t>(0xFFC678DD);
+  int32_t completion_kind_function = static_cast<int32_t>(0xFF61AFEF);
+  int32_t completion_kind_variable = static_cast<int32_t>(0xFFE5C07B);
+  int32_t completion_kind_class = static_cast<int32_t>(0xFFE06C75);
+  int32_t completion_kind_interface = static_cast<int32_t>(0xFF56B6C2);
+  int32_t completion_kind_module = static_cast<int32_t>(0xFFD19A66);
+  int32_t completion_kind_property = static_cast<int32_t>(0xFF98C379);
+  int32_t completion_kind_snippet = static_cast<int32_t>(0xFFBE5046);
+  int32_t completion_kind_text = static_cast<int32_t>(0xFF7A8494);
+  // Built-in context menu panel.
+  int32_t context_menu_background = static_cast<int32_t>(0xFFFFFFFF);
+  int32_t context_menu_border = static_cast<int32_t>(0xFFDDDDDD);
+  int32_t context_menu_item_text = static_cast<int32_t>(0xFF1F1F1F);
+  // Core render colors (se::EditorRenderColors).
+  int32_t text_foreground = static_cast<int32_t>(0xFF1F1F1F);
+  int32_t link_foreground = static_cast<int32_t>(0xFF4C9DFF);
+  int32_t active_link_foreground = static_cast<int32_t>(0xFF4C9DFF);
+  int32_t codelens_foreground = static_cast<int32_t>(0xB0344A73);
+  int32_t active_codelens_foreground = static_cast<int32_t>(0xFF3A5FA0);
+  int32_t diff_added_line_background = static_cast<int32_t>(0x1FA6E22E);
+  int32_t diff_removed_line_background = static_cast<int32_t>(0x1FF92672);
+  int32_t diff_added_gutter_background = static_cast<int32_t>(0x2FA6E22E);
+  int32_t diff_removed_gutter_background = static_cast<int32_t>(0x2FF92672);
+  // Range-effect styles (se::EditorRangeEffectStyles); the selection
+  // background reuses `selection_background` above.
+  int32_t search_match_background = static_cast<int32_t>(0x33F59E0B);
+  int32_t search_current_background = static_cast<int32_t>(0x55F59E0B);
+  int32_t bracket_match_background = static_cast<int32_t>(0x260F766E);
+  int32_t ime_composition_underline = static_cast<int32_t>(0xFF2563EB);
+  int32_t document_highlight_text = static_cast<int32_t>(0x142563EB);
+  int32_t document_highlight_read = static_cast<int32_t>(0x1C2563EB);
+  int32_t document_highlight_write = static_cast<int32_t>(0x282563EB);
+  int32_t diagnostic_error_underline = static_cast<int32_t>(0xFFDC2626);
+  int32_t diagnostic_warning_underline = static_cast<int32_t>(0xFFD97706);
+  int32_t diagnostic_info_underline = static_cast<int32_t>(0xFF0EA5E9);
+  int32_t diagnostic_hint_underline = static_cast<int32_t>(0xFF64748B);
+  // Syntax highlighting colors registered for the SweetLine style ids.
+  int32_t syntax_keyword = static_cast<int32_t>(0xFF0000FF);
+  int32_t syntax_type = static_cast<int32_t>(0xFF267F99);
+  int32_t syntax_class = static_cast<int32_t>(0xFF267F99);
+  int32_t syntax_function = static_cast<int32_t>(0xFF795E26);
+  int32_t syntax_variable = static_cast<int32_t>(0xFF001080);
+  int32_t syntax_string = static_cast<int32_t>(0xFFA31515);
+  int32_t syntax_number = static_cast<int32_t>(0xFF098658);
+  int32_t syntax_comment = static_cast<int32_t>(0xFF008000);
+  int32_t syntax_preprocessor = static_cast<int32_t>(0xFF9B4F96);
+  int32_t syntax_builtin = static_cast<int32_t>(0xFF0000FF);
+  int32_t syntax_punctuation = static_cast<int32_t>(0xFF777777);
+  int32_t syntax_annotation = static_cast<int32_t>(0xFFB35C00);
+  int32_t syntax_url = static_cast<int32_t>(0xFF0B5CAD);
+  // Rainbow bracket depth colors (style ids 100..107). The casts keep the
+  // 0xFF...... literals from narrowing inside the std::array braced init.
+  std::array<int32_t, 8> rainbow_brackets = {
+      static_cast<int32_t>(0xFFDC322F), static_cast<int32_t>(0xFF268BD2),
+      static_cast<int32_t>(0xFF859900), static_cast<int32_t>(0xFFB58900),
+      static_cast<int32_t>(0xFFCB4B16), static_cast<int32_t>(0xFF6C71C4),
+      static_cast<int32_t>(0xFF2AA198), static_cast<int32_t>(0xFFD33682),
+  };
+};
+
 // Declarative configuration for a SweetEditor instance.
 struct SweetEditorOptions {
   std::string initial_text;
@@ -230,6 +332,9 @@ struct SweetEditorOptions {
   float content_start_padding = 0.0F;
   // Host gutter icons (breakpoints, errors) merged during decoration refresh.
   GutterIconProvider gutter_icon_provider;
+  // NOTE(apitab local patch): host-injected colors; the default palette is the
+  // upstream hardcoded light theme, so omitting it preserves behavior.
+  SweetEditorPalette palette;
 };
 
 // A HuxerUI code editor backed by the SweetEditor core (3dparty/SweetEditor).
diff --git a/src/sweet_editor/sweet_editor.cpp b/src/sweet_editor/sweet_editor.cpp
index b7a01b5..c6dd86d 100644
--- a/src/sweet_editor/sweet_editor.cpp
+++ b/src/sweet_editor/sweet_editor.cpp
@@ -233,7 +233,16 @@ se::GestureEvent ToGestureEvent(const PointerEvent& event) {
   return out;
 }
 
-se::GestureEvent ToWheelGestureEvent(const ScrollEvent& event) {
+// NOTE(apitab local patch): HuxerUI 937efb1 renamed the pre-default wheel event
+// from ScrollEvent to ScrollInputEvent while preserving delta_x/delta_y. The
+// component still supports the released 0.2.0 SDK through CMake feature detection.
+#ifdef SWEETEDIT_CORE_HAS_SCROLL_INPUT_EVENT
+using HuxerScrollInputEvent = ScrollInputEvent;
+#else
+using HuxerScrollInputEvent = ScrollEvent;
+#endif
+
+se::GestureEvent ToWheelGestureEvent(const HuxerScrollInputEvent& event) {
   se::GestureEvent out;
   out.type = se::EventType::MOUSE_WHEEL;
   out.wheel_delta_x = event.delta_x;
@@ -242,18 +251,10 @@ se::GestureEvent ToWheelGestureEvent(const ScrollEvent& event) {
 }
 
 // ---- Theme defaults --------------------------------------------------------
-constexpr int32_t kEditorBackground = 0xFFFFFFFF;
-constexpr int32_t kGutterBackground = 0xFFF2F3F5;
-constexpr int32_t kLineNumberColor = 0xFF9AA0A6;
-constexpr int32_t kCursorColor = 0xFF1F1F1F;
-constexpr int32_t kSelectionBackground = 0x554A90E2;
-constexpr int32_t kCurrentLineBackground = 0x0F000000;
-constexpr int32_t kGuideColor = 0xFFC8C8C8;
-constexpr int32_t kSeparatorColor = 0xFFB0B7C3;
-constexpr int32_t kInlayHintBackground = 0x143B82F6;
-constexpr int32_t kInlayHintText = 0xB0344A73;
-constexpr int32_t kFoldPlaceholderBackground = 0x2E748DB0;
-constexpr int32_t kFoldPlaceholderText = 0xFF284A70;
+// NOTE(apitab local patch): all editor colors moved into SweetEditorPalette
+// (public header) so the host can inject a theme; the constants below are the
+// remaining non-color defaults. Palette defaults reproduce these values.
+constexpr float kCursorWidth = 2.0F;
 
 // Auto-closing bracket pairs enabled by default (reference platform ships
 // with these); hosts may override via SweetEditorOptions.auto_closing_pairs.
@@ -262,16 +263,9 @@ const std::vector<std::pair<char32_t, char32_t>> kDefaultAutoClosingPairs = {
     {U'{', U'}'},
     {U'[', U']'},
 };
-constexpr int32_t kGutterIconColor = 0xFF267F99;
-constexpr float kCursorWidth = 2.0F;
 
 // Completion panel, matching the SweetEditor reference light theme and
 // dimensions (Android CompletionPopupController / EditorTheme.java).
-constexpr int32_t kCompletionPanelBackground = 0xF0FAFBFD;
-constexpr int32_t kCompletionPanelBorder = 0x30A0A8B8;
-constexpr int32_t kCompletionSelectedBackground = 0x3D3B82F6;
-constexpr int32_t kCompletionLabelColor = 0xFF1F2937;
-constexpr int32_t kCompletionDetailColor = 0xFF8A94A6;
 constexpr float kCompletionPanelWidth = 300.0F;
 constexpr float kCompletionPanelMinWidth = 120.0F;
 constexpr float kCompletionRowHeight = 32.0F;
@@ -288,27 +282,28 @@ constexpr float kCompletionLabelSize = 13.0F;
 constexpr float kCompletionDetailSize = 11.0F;
 constexpr float kCompletionBadgeSizePx = 10.0F;
 
-int32_t CompletionKindColor(CompletionItemKind kind) {
+// NOTE(apitab local patch): kind badge colors come from the injected palette.
+int32_t CompletionKindColor(CompletionItemKind kind, const SweetEditorPalette& palette) {
   switch (kind) {
   case CompletionItemKind::Keyword:
-    return 0xFFC678DD;
+    return palette.completion_kind_keyword;
   case CompletionItemKind::Function:
-    return 0xFF61AFEF;
+    return palette.completion_kind_function;
   case CompletionItemKind::Variable:
-    return 0xFFE5C07B;
+    return palette.completion_kind_variable;
   case CompletionItemKind::Class:
-    return 0xFFE06C75;
+    return palette.completion_kind_class;
   case CompletionItemKind::Interface:
-    return 0xFF56B6C2;
+    return palette.completion_kind_interface;
   case CompletionItemKind::Module:
-    return 0xFFD19A66;
+    return palette.completion_kind_module;
   case CompletionItemKind::Property:
-    return 0xFF98C379;
+    return palette.completion_kind_property;
   case CompletionItemKind::Snippet:
-    return 0xFFBE5046;
+    return palette.completion_kind_snippet;
   case CompletionItemKind::Text:
   default:
-    return 0xFF7A8494;
+    return palette.completion_kind_text;
   }
 }
 
@@ -352,40 +347,42 @@ std::vector<se::BracketPair> MakeBracketPairs(const std::vector<std::pair<char32
   return result;
 }
 
-se::EditorRenderColors MakeRenderColors() {
+// NOTE(apitab local patch): render colors / range-effect styles are sourced
+// from the injected palette instead of hardcoded light-theme literals.
+se::EditorRenderColors MakeRenderColors(const SweetEditorPalette& palette) {
   se::EditorRenderColors colors;
-  colors.text_foreground = 0xFF1F1F1F;
-  colors.link_foreground = 0xFF4C9DFF;
-  colors.active_link_foreground = 0xFF4C9DFF;
-  colors.codelens_foreground = 0xB0344A73;
-  colors.active_codelens_foreground = 0xFF3A5FA0;
-  colors.diff_added_line_background = 0x1FA6E22E;
-  colors.diff_removed_line_background = 0x1FF92672;
-  colors.diff_added_gutter_background = 0x2FA6E22E;
-  colors.diff_removed_gutter_background = 0x2FF92672;
+  colors.text_foreground = palette.text_foreground;
+  colors.link_foreground = palette.link_foreground;
+  colors.active_link_foreground = palette.active_link_foreground;
+  colors.codelens_foreground = palette.codelens_foreground;
+  colors.active_codelens_foreground = palette.active_codelens_foreground;
+  colors.diff_added_line_background = palette.diff_added_line_background;
+  colors.diff_removed_line_background = palette.diff_removed_line_background;
+  colors.diff_added_gutter_background = palette.diff_added_gutter_background;
+  colors.diff_removed_gutter_background = palette.diff_removed_gutter_background;
   return colors;
 }
 
-se::EditorRangeEffectStyles MakeRangeEffectStyles() {
+se::EditorRangeEffectStyles MakeRangeEffectStyles(const SweetEditorPalette& palette) {
   se::EditorRangeEffectStyles styles;
-  styles.selection.background_color = kSelectionBackground;
-  styles.search_match.background_color = 0x33F59E0B;
-  styles.search_current.background_color = 0x55F59E0B;
-  styles.bracket_match.background_color = 0x260F766E;
-  styles.ime_composition.underline_color = 0xFF2563EB;
+  styles.selection.background_color = palette.selection_background;
+  styles.search_match.background_color = palette.search_match_background;
+  styles.search_current.background_color = palette.search_current_background;
+  styles.bracket_match.background_color = palette.bracket_match_background;
+  styles.ime_composition.underline_color = palette.ime_composition_underline;
   styles.ime_composition.underline_style = se::RangeEffectUnderlineStyle::SOLID;
 
   // SweetEditor light-theme reference values (platform/Android EditorTheme.java).
-  styles.document_highlight_text.background_color = 0x142563EB;
-  styles.document_highlight_read.background_color = 0x1C2563EB;
-  styles.document_highlight_write.background_color = 0x282563EB;
-  styles.diagnostic_error.underline_color = 0xFFDC2626;
+  styles.document_highlight_text.background_color = palette.document_highlight_text;
+  styles.document_highlight_read.background_color = palette.document_highlight_read;
+  styles.document_highlight_write.background_color = palette.document_highlight_write;
+  styles.diagnostic_error.underline_color = palette.diagnostic_error_underline;
   styles.diagnostic_error.underline_style = se::RangeEffectUnderlineStyle::WAVY;
-  styles.diagnostic_warning.underline_color = 0xFFD97706;
+  styles.diagnostic_warning.underline_color = palette.diagnostic_warning_underline;
   styles.diagnostic_warning.underline_style = se::RangeEffectUnderlineStyle::WAVY;
-  styles.diagnostic_info.underline_color = 0xFF0EA5E9;
+  styles.diagnostic_info.underline_color = palette.diagnostic_info_underline;
   styles.diagnostic_info.underline_style = se::RangeEffectUnderlineStyle::WAVY;
-  styles.diagnostic_hint.underline_color = 0xFF64748B;
+  styles.diagnostic_hint.underline_color = palette.diagnostic_hint_underline;
   styles.diagnostic_hint.underline_style = se::RangeEffectUnderlineStyle::WAVY;
   return styles;
 }
@@ -402,7 +399,9 @@ public:
       std::function<void(const std::vector<se::TextChange>&)> on_text_committed,
       std::function<void()> on_edit,
       std::function<bool()> on_tab,
-      bool read_only
+      bool read_only,
+      // NOTE(apitab local patch): selection handle accent from the palette.
+      int32_t selection_handle_color
   )
       : core_(std::move(core)),
         document_(std::move(document)),
@@ -411,7 +410,8 @@ public:
         on_text_committed_(std::move(on_text_committed)),
         on_edit_(std::move(on_edit)),
         on_tab_(std::move(on_tab)),
-        read_only_(read_only) {}
+        read_only_(read_only),
+        selection_handle_color_(selection_handle_color) {}
 
   TextInputConfiguration Configuration() const override {
     TextInputConfiguration configuration;
@@ -690,7 +690,8 @@ public:
   }
 
   Color SelectionHandleColor() const noexcept override {
-    return Argb(0xFF1F6FEB);
+    // NOTE(apitab local patch): injected via SweetEditorPalette.selection_handle.
+    return Argb(selection_handle_color_);
   }
 
 private:
@@ -850,6 +851,8 @@ private:
   std::function<bool()> on_tab_;
   std::function<void()> on_diff_edit_;
   bool read_only_;
+  // NOTE(apitab local patch): injected selection handle accent (ARGB).
+  int32_t selection_handle_color_;
   PlatformClipboard* clipboard_{nullptr};
   TextInputSessionId session_id_ = 0;
   std::uint64_t revision_ = 0;
@@ -874,6 +877,9 @@ public:
       SweetEditorController controller
   )
       : font_size_(options.font_size),
+        // NOTE(apitab local patch): the palette is copied out of options so
+        // every paint path below reads the host-injected colors.
+        palette_(options.palette),
         invalidate_(std::move(invalidate)),
         events_(std::move(events)),
         controller_(std::move(controller)),
@@ -892,8 +898,8 @@ public:
     se::EditorOptions core_options;
     core_ = std::make_shared<se::EditorCore>(
         std::make_shared<HuxeruiTextMeasurer>(measurer, font_size_), core_options);
-    core_->setEditorRenderColors(MakeRenderColors());
-    core_->setEditorRangeEffectStyles(MakeRangeEffectStyles());
+    core_->setEditorRenderColors(MakeRenderColors(palette_));
+    core_->setEditorRangeEffectStyles(MakeRangeEffectStyles(palette_));
     core_->setLineSpacing(options.line_spacing_add, options.line_spacing_mult);
     core_->setReadOnly(options.read_only);
 
@@ -957,7 +963,7 @@ public:
 
     highlighter_ = std::make_shared<SweetLineHighlighter>(
         std::move(syntax_json), options.initial_text, options.document_key);
-    highlighter_->RegisterStyles(*core_);
+    highlighter_->RegisterStyles(*core_, palette_);
     highlighter_->SetHostDecorationProviders(options.decoration_providers);
     highlighter_->SetGutterIconProvider(options.gutter_icon_provider);
     // Publish fold regions once, right after the document loads, so fold
@@ -974,7 +980,8 @@ public:
           }
         },
         [this]() { return HandleTab(); },
-        options.read_only);
+        options.read_only,
+        palette_.selection_handle);
     text_input_client_->SetDiffEditHook([this]() {
       if (current_diff_original_.empty()) {
         return;
@@ -1190,10 +1197,11 @@ public:
       return;
     }
     const se::Rect& rect = context_menu_.panel_rect;
-    paint.DrawRect(ToRect(rect), Argb(0xFFFFFFFF), CornerRadii(6.0F));
-    paint.DrawBorder(ToRect(rect), Argb(0xFFDDDDDD), 1.0F, CornerRadii(6.0F));
+    // NOTE(apitab local patch): context menu colors from the injected palette.
+    paint.DrawRect(ToRect(rect), Argb(palette_.context_menu_background), CornerRadii(6.0F));
+    paint.DrawBorder(ToRect(rect), Argb(palette_.context_menu_border), StrokeStyle{.width = 1.0F}, CornerRadii(6.0F));
     const float row_height = 30.0F;
-    const TextStyle item_style{Font::System(13.0F), Argb(0xFF1F1F1F), TextDecoration::None};
+    const TextStyle item_style{Font::System(13.0F), Argb(palette_.context_menu_item_text), TextDecoration::None};
     float y = rect.origin.y + 4.0F;
     for (const ContextMenuEntry& entry : context_menu_.entries) {
       const float baseline = y + (row_height + font_metrics_.descent - font_metrics_.ascent) * 0.5F;
@@ -1504,7 +1512,7 @@ public:
     }
   }
 
-  bool HandleScroll(const ScrollEvent& event) {
+  bool HandleScroll(const HuxerScrollInputEvent& event) {
     const se::EditorActionResult result = core_->handleGestureEvent(ToWheelGestureEvent(event));
     if (result.selection_changed || result.cursor_changed) {
       text_input_client_->NotifySelectionChanged();
@@ -1900,22 +1908,22 @@ private:
     const float ascent = font_metrics_.ascent;
     const float line_height = ascent + font_metrics_.descent;
 
-    paint.DrawRect(Rect{0.0F, 0.0F, width, height}, Argb(kEditorBackground));
+    paint.DrawRect(Rect{0.0F, 0.0F, width, height}, Argb(palette_.editor_background));
 
     // The gutter (line-number column) sits on its own background strip; the
     // core crops text runs with a margin that hides under this strip, so it
     // must be painted or cropped characters bleed into the line-number area.
     if (model.split_x > 0.0F) {
-      paint.DrawRect(Rect{0.0F, 0.0F, model.split_x, height}, Argb(kGutterBackground));
+      paint.DrawRect(Rect{0.0F, 0.0F, model.split_x, height}, Argb(palette_.gutter_background));
       // The split line separates the gutter from the text area (reference
       // renderer draws it when splitLineVisible is set).
       if (model.split_line_visible) {
-        paint.DrawRect(Rect{model.split_x - 1.0F, 0.0F, 1.0F, height}, Argb(0xFFE3E5E8));
+        paint.DrawRect(Rect{model.split_x - 1.0F, 0.0F, 1.0F, height}, Argb(palette_.gutter_split_line));
       }
     }
 
     if (model.current_line_render_mode == se::CurrentLineRenderMode::BACKGROUND) {
-      paint.DrawRect(Rect{0.0F, model.current_line.y, width, LineHeight(model)}, Argb(kCurrentLineBackground));
+      paint.DrawRect(Rect{0.0F, model.current_line.y, width, LineHeight(model)}, Argb(palette_.current_line_background));
     }
 
     for (const se::RangeEffectRenderItem& effect : model.range_effects) {
@@ -1924,7 +1932,7 @@ private:
       }
     }
 
-    const TextStyle line_number_style{Font::Monospace(font_size_), Argb(kLineNumberColor), TextDecoration::None};
+    const TextStyle line_number_style{Font::Monospace(font_size_), Argb(palette_.line_number), TextDecoration::None};
     for (const se::VisualLine& line : model.lines) {
       // line_number_position.y is the text baseline; the row background spans
       // from the line top (baseline - ascent) down the full line height.
@@ -1989,7 +1997,7 @@ private:
     if (caret_visible) {
       paint.DrawRect(
           Rect{model.cursor.position.x, model.cursor.position.y, kCursorWidth, model.cursor.height},
-          Argb(kCursorColor));
+          Argb(palette_.cursor));
     }
 
     // Handles are drawn here (single owner) because the HuxerUI selection
@@ -2018,8 +2026,8 @@ private:
     // the line bottom (reference drag-handle anchor).
     const float line_top = handle.position.y;
     const float line_bottom = handle.position.y + handle.height;
-    paint.DrawRect(Rect{cx - 1.0F, line_top, 2.0F, std::max(1.0F, line_bottom - line_top)}, Argb(0xFF1F6FEB));
-    paint.DrawCircle(Point{cx, line_bottom + radius * 0.8F}, radius, Argb(0xFF1F6FEB));
+    paint.DrawRect(Rect{cx - 1.0F, line_top, 2.0F, std::max(1.0F, line_bottom - line_top)}, Argb(palette_.selection_handle));
+    paint.DrawCircle(Point{cx, line_bottom + radius * 0.8F}, radius, Argb(palette_.selection_handle));
   }
 
   void DrawScrollbar(PaintContext& paint, const se::ScrollbarModel& bar) {
@@ -2027,7 +2035,14 @@ private:
     if (thumb.width <= 0.0F || thumb.height <= 0.0F) {
       return;
     }
-    paint.DrawRect(thumb, Color{0.0F, 0.0F, 0.0F, std::clamp(bar.alpha, 0.0F, 1.0F) * 0.45F}, CornerRadii(4.0F));
+    // NOTE(apitab local patch): thumb color from the palette; its alpha is
+    // additionally scaled by the core-reported scrollbar alpha.
+    const Color thumb_color = Argb(palette_.scrollbar_thumb);
+    paint.DrawRect(
+        thumb,
+        Color{thumb_color.red, thumb_color.green, thumb_color.blue,
+              thumb_color.alpha * std::clamp(bar.alpha, 0.0F, 1.0F)},
+        CornerRadii(4.0F));
   }
 
   void DrawRun(PaintContext& paint, const se::VisualRun& run, float line_height) {
@@ -2048,7 +2063,7 @@ private:
       const float center_y = run.y - ascent * 0.35F;
       const float spacing = std::max(2.0F, char_width_ * 0.5F);
       for (float x = run.x + spacing; x < run.x + run.width; x += char_width_) {
-        paint.DrawCircle(Point{x, center_y}, 1.2F, Color{0.0F, 0.0F, 0.0F, 0.25F});
+        paint.DrawCircle(Point{x, center_y}, 1.2F, Argb(palette_.whitespace_marker));
       }
       return;
     }
@@ -2062,7 +2077,7 @@ private:
       path.LineTo(Point{x + char_width_ * 0.45F, mid_y - 3.0F});
       path.MoveTo(Point{x + char_width_ * 0.6F, mid_y});
       path.LineTo(Point{x + char_width_ * 0.45F, mid_y + 3.0F});
-      paint.StrokePath(path, Color{0.0F, 0.0F, 0.0F, 0.3F}, 1.0F);
+      paint.StrokePath(path, Argb(palette_.whitespace_symbol), StrokeStyle{.width = 1.0F});
       return;
     }
     if (run.type == se::VisualRunType::NEWLINE) {
@@ -2074,7 +2089,7 @@ private:
       path.LineTo(Point{x + 8.0F, mid_y});
       path.LineTo(Point{x + 8.0F, mid_y + 4.0F});
       path.LineTo(Point{x + 4.0F, mid_y + 4.0F});
-      paint.StrokePath(path, Color{0.0F, 0.0F, 0.0F, 0.3F}, 1.0F);
+      paint.StrokePath(path, Argb(palette_.whitespace_symbol), StrokeStyle{.width = 1.0F});
       return;
     }
     if (run.text.empty()) {
@@ -2094,7 +2109,7 @@ private:
     }
 
     if (run.type == se::VisualRunType::PHANTOM_TEXT) {
-      TextStyle phantom{Font::Monospace(font_size_).WithSlant(FontSlant::Italic), Color{0.6F, 0.6F, 0.6F, 0.6F},
+      TextStyle phantom{Font::Monospace(font_size_).WithSlant(FontSlant::Italic), Argb(palette_.phantom_text),
                         TextDecoration::None};
       paint.DrawTextRun(Rect{run.x, top, run.width, height}, Point{run.x, run.y}, text, phantom);
       return;
@@ -2111,10 +2126,10 @@ private:
       return;
     }
     const Rect background{run.x + margin, top, run.width - margin * 2.0F, height};
-    paint.DrawRect(background, Argb(kInlayHintBackground), CornerRadii(height * 0.2F));
+    paint.DrawRect(background, Argb(palette_.inlay_hint_background), CornerRadii(height * 0.2F));
     if (!run.text.empty()) {
       const float text_x = run.x + margin + run.padding;
-      const TextStyle style{Font::System(font_size_ * 0.9F), Argb(kInlayHintText), TextDecoration::None};
+      const TextStyle style{Font::System(font_size_ * 0.9F), Argb(palette_.inlay_hint_text), TextDecoration::None};
       paint.DrawTextRun(Rect{text_x, top, run.width, height}, Point{text_x, run.y}, Utf16ToUtf8(run.text), style);
     }
   }
@@ -2122,10 +2137,10 @@ private:
   void DrawFoldPlaceholder(PaintContext& paint, const se::VisualRun& run, float top, float height) {
     const float margin = run.margin;
     const Rect background{run.x + margin, top, run.width - margin * 2.0F, height};
-    paint.DrawRect(background, Argb(kFoldPlaceholderBackground), CornerRadii(height * 0.2F));
+    paint.DrawRect(background, Argb(palette_.fold_placeholder_background), CornerRadii(height * 0.2F));
     if (!run.text.empty()) {
       const float text_x = run.x + margin + run.padding;
-      const TextStyle style{Font::Monospace(font_size_), Argb(kFoldPlaceholderText), TextDecoration::None};
+      const TextStyle style{Font::Monospace(font_size_), Argb(palette_.fold_placeholder_text), TextDecoration::None};
       paint.DrawTextRun(Rect{text_x, top, run.width, height}, Point{text_x, run.y}, Utf16ToUtf8(run.text), style);
     }
   }
@@ -2143,14 +2158,14 @@ private:
       diamond.LineTo(Point{center.x, rect.y + rect.height});
       diamond.LineTo(Point{rect.x, center.y});
       diamond.Close();
-      paint.StrokePath(diamond, Argb(kGutterIconColor), 1.5F);
+      paint.StrokePath(diamond, Argb(palette_.gutter_icon), StrokeStyle{.width = 1.5F});
       return;
     }
     if (icon.icon_id == 2) {
-      paint.DrawCircle(center, rect.width * 0.5F, Argb(kGutterIconColor));
+      paint.DrawCircle(center, rect.width * 0.5F, Argb(palette_.gutter_icon));
       return;
     }
-    paint.DrawCircle(center, rect.width * 0.35F, Argb(kGutterIconColor));
+    paint.DrawCircle(center, rect.width * 0.35F, Argb(palette_.gutter_icon));
   }
 
   void DrawFoldMarker(PaintContext& paint, const se::FoldMarkerRenderItem& marker) {
@@ -2165,7 +2180,7 @@ private:
       path.LineTo(Point{rect.x + rect.width * 0.5F, rect.y + rect.height * 0.75F});
       path.LineTo(Point{rect.x + rect.width * 0.8F, rect.y + rect.height * 0.35F});
     }
-    paint.StrokePath(path, Argb(kLineNumberColor), 1.5F);
+    paint.StrokePath(path, Argb(palette_.line_number), StrokeStyle{.width = 1.5F});
   }
 
   void DrawUnderline(
@@ -2184,7 +2199,7 @@ private:
         up = !up;
       }
       path.LineTo(Point{r.x + r.width, baseline + (up ? -kAmplitude : kAmplitude)});
-      paint.StrokePath(path, Argb(color), 1.0F);
+      paint.StrokePath(path, Argb(color), StrokeStyle{.width = 1.0F});
       return;
     }
     if (style == se::RangeEffectUnderlineStyle::DASHED) {
@@ -2202,7 +2217,7 @@ private:
   }
 
   void DrawGuideSegment(PaintContext& paint, const se::GuideSegment& segment) {
-    const int32_t color = segment.type == se::GuideType::SEPARATOR ? kSeparatorColor : kGuideColor;
+    const int32_t color = segment.type == se::GuideType::SEPARATOR ? palette_.separator : palette_.guide;
     // Separator and bracket lines are lighter than indent guides (reference
     // renderer uses a separate paint for separators).
     const float width = segment.type == se::GuideType::SEPARATOR ? 1.0F : 1.0F;
@@ -2210,7 +2225,7 @@ private:
       Path path;
       path.MoveTo(a);
       path.LineTo(b);
-      paint.StrokePath(path, Argb(color), width);
+      paint.StrokePath(path, Argb(color), StrokeStyle{.width = width});
     };
 
     // DOUBLE style draws a pair of parallel lines straddling the segment.
@@ -2487,8 +2502,8 @@ private:
 
   void DrawCompletionPanel(PaintContext& paint, const se::Rect& rect) {
     const Rect panel{rect.origin.x, rect.origin.y, rect.width, rect.height};
-    paint.DrawRect(panel, Argb(kCompletionPanelBackground), CornerRadii(12.0F));
-    paint.DrawBorder(panel, Argb(kCompletionPanelBorder), 1.0F, CornerRadii(12.0F));
+    paint.DrawRect(panel, Argb(palette_.completion_panel_background), CornerRadii(12.0F));
+    paint.DrawBorder(panel, Argb(palette_.completion_panel_border), StrokeStyle{.width = 1.0F}, CornerRadii(12.0F));
 
     const float content_left = rect.origin.x + kCompletionPanelPaddingH + kCompletionRowPaddingH;
     float row_center_y = rect.origin.y + kCompletionPanelPaddingV + kCompletionRowHeight * 0.5F;
@@ -2500,7 +2515,7 @@ private:
         paint.DrawRect(
             Rect{rect.origin.x + kCompletionPanelPaddingH, row_center_y - kCompletionRowHeight * 0.5F,
                  rect.width - kCompletionPanelPaddingH * 2.0F, kCompletionRowHeight},
-            Argb(kCompletionSelectedBackground),
+            Argb(palette_.completion_selected_background),
             CornerRadii(6.0F));
       }
 
@@ -2509,11 +2524,11 @@ private:
       const float badge_x = content_left;
       paint.DrawRect(
           Rect{badge_x, badge_center_y - kCompletionBadgeSize * 0.5F, kCompletionBadgeSize, kCompletionBadgeSize},
-          Argb(CompletionKindColor(item.kind)),
+          Argb(CompletionKindColor(item.kind, palette_)),
           CornerRadii(4.0F));
       const std::string letter = CompletionKindLetter(item.kind);
-      const TextStyle badge_style{Font::System(kCompletionBadgeSizePx).WithWeight(FontWeight::Bold), Argb(0xFFFFFFFF),
-                                  TextDecoration::None};
+      const TextStyle badge_style{Font::System(kCompletionBadgeSizePx).WithWeight(FontWeight::Bold),
+                                  Argb(palette_.completion_badge_text), TextDecoration::None};
       const float badge_baseline = CenterBaseline(badge_center_y, completion_badge_metrics_);
       const float badge_text_x = badge_x + (kCompletionBadgeSize - completion_badge_char_advance_) * 0.5F;
       paint.DrawTextRun(
@@ -2530,7 +2545,7 @@ private:
                rect.width - (label_x - rect.origin.x) - kCompletionRowPaddingH, kCompletionRowHeight},
           Point{label_x, label_baseline},
           item.label,
-          TextStyle{Font::System(kCompletionLabelSize), Argb(kCompletionLabelColor), TextDecoration::None});
+          TextStyle{Font::System(kCompletionLabelSize), Argb(palette_.completion_label), TextDecoration::None});
 
       if (!item.detail.empty()) {
         // Estimated advance (reference renders an 11sp detail at the row end).
@@ -2543,7 +2558,7 @@ private:
               Rect{detail_x, row_center_y - kCompletionRowHeight * 0.5F, detail_advance, kCompletionRowHeight},
               Point{detail_x, detail_baseline},
               item.detail,
-              TextStyle{Font::System(kCompletionDetailSize), Argb(kCompletionDetailColor), TextDecoration::None});
+              TextStyle{Font::System(kCompletionDetailSize), Argb(palette_.completion_detail), TextDecoration::None});
         }
       }
 
@@ -2561,6 +2576,8 @@ private:
   FontMetrics font_metrics_;
   float font_size_;
   float char_width_;
+  // NOTE(apitab local patch): host-injected colors (SweetEditorOptions.palette).
+  SweetEditorPalette palette_;
   CompletionProvider completion_provider_;
   std::function<bool(const std::string&)> completion_trigger_characters_;
   FontMetrics completion_label_metrics_;
@@ -2844,11 +2861,13 @@ struct SweetEditorBehavior {
       return changed ? PointerResult::Handled : PointerResult::Ignored;
     }
 
-    void OnKey(MountedNode& node, const KeyEvent& event) override {
+    bool OnKey(MountedNode& node, const KeyEvent& event) override {
       static_cast<void>(node);
       if (holder_->HandleKey(event)) {
         InvalidatePaint();
+        return true;
       }
+      return false;
     }
 
     void OnFocusChanged(MountedNode& node, bool focused) override {
diff --git a/src/sweet_editor/sweetline_highlighter.cpp b/src/sweet_editor/sweetline_highlighter.cpp
index 17a8a45..1c41ed3 100644
--- a/src/sweet_editor/sweetline_highlighter.cpp
+++ b/src/sweet_editor/sweetline_highlighter.cpp
@@ -123,27 +123,25 @@ SweetLineHighlighter::SweetLineHighlighter(std::string syntax_json, std::string
   analyzer_ = engine_->loadDocument(document_);
 }
 
-void SweetLineHighlighter::RegisterStyles(sweeteditor::EditorCore& core) {
-  core.registerTextStyle(kStyleKeyword, Style(static_cast<int32_t>(0xFF0000FF)));
-  core.registerTextStyle(kStyleType, Style(static_cast<int32_t>(0xFF267F99)));
-  core.registerTextStyle(kStyleClass, Style(static_cast<int32_t>(0xFF267F99)));
-  core.registerTextStyle(kStyleFunction, Style(static_cast<int32_t>(0xFF795E26)));
-  core.registerTextStyle(kStyleVariable, Style(static_cast<int32_t>(0xFF001080)));
-  core.registerTextStyle(kStyleString, Style(static_cast<int32_t>(0xFFA31515)));
-  core.registerTextStyle(kStyleNumber, Style(static_cast<int32_t>(0xFF098658)));
-  core.registerTextStyle(kStyleComment, Style(static_cast<int32_t>(0xFF008000)));
-  core.registerTextStyle(kStylePreprocessor, Style(static_cast<int32_t>(0xFF9B4F96)));
-  core.registerTextStyle(kStyleBuiltin, Style(static_cast<int32_t>(0xFF0000FF)));
-  core.registerTextStyle(kStylePunctuation, Style(static_cast<int32_t>(0xFF777777)));
-  core.registerTextStyle(kStyleAnnotation, Style(static_cast<int32_t>(0xFFB35C00)));
-  core.registerTextStyle(kStyleUrl, Style(static_cast<int32_t>(0xFF0B5CAD)));
+// NOTE(apitab local patch): colors come from the injected SweetEditorPalette
+// (defaults reproduce the upstream hardcoded light theme below).
+void SweetLineHighlighter::RegisterStyles(sweeteditor::EditorCore& core, const SweetEditorPalette& palette) {
+  core.registerTextStyle(kStyleKeyword, Style(palette.syntax_keyword));
+  core.registerTextStyle(kStyleType, Style(palette.syntax_type));
+  core.registerTextStyle(kStyleClass, Style(palette.syntax_class));
+  core.registerTextStyle(kStyleFunction, Style(palette.syntax_function));
+  core.registerTextStyle(kStyleVariable, Style(palette.syntax_variable));
+  core.registerTextStyle(kStyleString, Style(palette.syntax_string));
+  core.registerTextStyle(kStyleNumber, Style(palette.syntax_number));
+  core.registerTextStyle(kStyleComment, Style(palette.syntax_comment));
+  core.registerTextStyle(kStylePreprocessor, Style(palette.syntax_preprocessor));
+  core.registerTextStyle(kStyleBuiltin, Style(palette.syntax_builtin));
+  core.registerTextStyle(kStylePunctuation, Style(palette.syntax_punctuation));
+  core.registerTextStyle(kStyleAnnotation, Style(palette.syntax_annotation));
+  core.registerTextStyle(kStyleUrl, Style(palette.syntax_url));
   // Rainbow bracket depth palette (style ids 100..107).
-  static constexpr uint32_t kRainbowColors[8] = {
-      0xFFDC322F, 0xFF268BD2, 0xFF859900, 0xFFB58900,
-      0xFFCB4B16, 0xFF6C71C4, 0xFF2AA198, 0xFFD33682,
-  };
   for (uint32_t index = 0; index < 8; ++index) {
-    core.registerTextStyle(kRainbowBase + index, Style(static_cast<int32_t>(kRainbowColors[index])));
+    core.registerTextStyle(kRainbowBase + index, Style(palette.rainbow_brackets[index]));
   }
 }
 
diff --git a/src/sweet_editor/sweetline_highlighter.h b/src/sweet_editor/sweetline_highlighter.h
index cf93127..b1255a9 100644
--- a/src/sweet_editor/sweetline_highlighter.h
+++ b/src/sweet_editor/sweetline_highlighter.h
@@ -41,7 +41,9 @@ public:
   // loadDocument return null (and the analyzer unusable).
   SweetLineHighlighter(std::string syntax_json, std::string initial_text, std::string document_key);
 
-  void RegisterStyles(sweeteditor::EditorCore& core);
+  // NOTE(apitab local patch): style colors are injected via the palette so the
+  // host controls syntax highlighting colors (defaults = upstream light theme).
+  void RegisterStyles(sweeteditor::EditorCore& core, const SweetEditorPalette& palette);
   // Analyze enough lines to cover the current viewport and publish those spans.
   void RefreshVisible(sweeteditor::EditorCore& core);
   // Syntax-highlight-only path for scrolling: publishes spans but defers
