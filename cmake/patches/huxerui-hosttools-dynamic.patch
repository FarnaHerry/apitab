diff --git a/tools/codegen/CMakeLists.txt b/tools/codegen/CMakeLists.txt
index 47337af..7ed6ffc 100644
--- a/tools/codegen/CMakeLists.txt
+++ b/tools/codegen/CMakeLists.txt
@@ -17,7 +17,9 @@ target_compile_options(huxerui_codegen PRIVATE
         "$<$<NOT:$<CXX_COMPILER_ID:MSVC>>:-Wpedantic>"
 )
 
-if (CMAKE_SYSTEM_NAME STREQUAL "Linux")
+# apitab local patch: static libstdc++ is unavailable on the dev host
+# (libstdc++-static not installed); tools run on this machine only, link dynamically.
+if (CMAKE_SYSTEM_NAME STREQUAL "Linux" AND NOT APITAB_HOST_TOOLS_DYNAMIC)
     target_link_options(huxerui_codegen PRIVATE -static-libstdc++ -static-libgcc)
 endif ()
 
diff --git a/tools/resource_compiler/CMakeLists.txt b/tools/resource_compiler/CMakeLists.txt
index 37aa1cc..60da688 100644
--- a/tools/resource_compiler/CMakeLists.txt
+++ b/tools/resource_compiler/CMakeLists.txt
@@ -18,7 +18,9 @@ target_compile_options(huxerui_resource_compiler PRIVATE
         "$<$<NOT:$<CXX_COMPILER_ID:MSVC>>:-Wpedantic>"
 )
 
-if (CMAKE_SYSTEM_NAME STREQUAL "Linux")
+# apitab local patch: static libstdc++ is unavailable on the dev host
+# (libstdc++-static not installed); tools run on this machine only, link dynamically.
+if (CMAKE_SYSTEM_NAME STREQUAL "Linux" AND NOT APITAB_HOST_TOOLS_DYNAMIC)
     target_link_options(huxerui_resource_compiler PRIVATE -static-libstdc++ -static-libgcc)
 endif ()
 
