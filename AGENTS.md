# apitab agent workflow

- 每次修改完成后都必须重新编译：完成任何代码、CMake、资源或生成输入修改后，立即执行
  与改动相符的完整构建；通常为 `cmake --build build --target apitab --parallel $(nproc)`。
  只有 `apitab` 编译并链接成功，才能交付修改；仅静态检查或部分编译不算完成。
- 任务验证通过后，只暂存该任务实际修改的文件，创建语义清晰的 Git commit，并尝试推送
  当前分支上游。推送失败时记录直接原因即可；不得反复重试或把其他既有改动混进提交。
- 工作区可能包含其他人的未提交修改。先检查状态与差异，保留它们，不重置、不覆盖、不提交。

## 资源与 RAII（改代码前必读）

**任何"获取 → 使用 → 释放"的资源都必须由一个类型持有、在析构里释放。** 禁止在函数
末尾手写 cleanup，禁止把裸句柄（`int fd` / `HANDLE` / `pid_t` / `FILE*` / 裸指针）存成
成员或跨语句传递。本仓有常驻工作线程、常驻监视线程和会被取消的 UI 协程，一次
`std::bad_alloc`、一次提前 `return`、一次取消就足以让泄漏累积到进程结束。

覆盖范围与写法：

- **C 句柄**：curl、子进程与管道、`popen` 流、Win32 注册表键、单实例锁 fd/HANDLE、
  SQLite 连接与语句 —— 各自有小包装类型，用它们，不要绕过。
- **多步装配**（如 k6 spawn：建管道 → 建子进程 → 移交成员）用"局部 guard 持有 +
  成功后 `adopt()` / `release()` 移交成员"。这样中间任一步失败（返回 false、抛异常）
  都由 guard 自动收尾。
- **线程"谁起谁 join"**：起线程的构造函数要么把 worker 起齐，要么自己置停止位并 join
  已起的线程，再把异常抛出去。凑不齐就让异常逸出时，未完成构造的对象不会走析构，而
  `std::thread` 的析构撞上 joinable 线程会直接 `std::terminate`。
- **协程里的临时资源**必须由协程帧内的守卫持有：页面卸载 / TaskScope 取消会在
  `co_await` 悬挂点销毁协程帧，函数末尾那行清理不会执行。清理要能同步完成（如
  `huxerui::File::Delete()`），并保证析构不抛出。
- **析构兜底要彻底**：子进程代理类析构 = 强杀 + 回收（不留僵尸）；延迟初始化的句柄
  析构 = 若已创建就释放。

清单与既有包装：`docs/architecture.md` §3（资源所有权矩阵）与 §5（RAII 约定）；
完整规范见 `CLAUDE.md`「关键约定」第 9 条。新增或替换资源类型时，同步更新那张矩阵。
