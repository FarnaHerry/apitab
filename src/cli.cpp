// cli.cpp — 命令行模式实现（桩，待完整实现替换）。
#include "cli.h"

#include <string>
#include <vector>

namespace apitab::cli {

int run(const std::vector<std::string>& args) {
    (void)args;
    return 0;
}

} // namespace apitab::cli
